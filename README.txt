# Homework 5: Graph Traversals

## Files and Folders

* `src/'
  - `main.rs': contains the Rust code for the graph traversal program
* `Cargo.toml' - Cargo project description
* `.gitignore' - prevents target folder from being included in git repo
* `graph.dat', `graph2.dat', `bad_graph_duplicates.dat',
`bad_graph_missing_node.dat' - Files used when testing the graph traversal
program.
